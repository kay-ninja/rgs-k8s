apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: history-geoip
  namespace: rgs-core
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: hcloud-volumes  # change to your actual class
  volumeMode: Filesystem

