apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-server
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Release.Name }}
    component: server
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.server.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: {{ .Release.Name }}
    component: server
